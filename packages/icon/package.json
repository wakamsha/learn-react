{
  "name": "@learn-react/icon",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "reset": "rm -rf dist/; mkdir -p dist/",
    "generate": "node ./bin/build",
    "format": "run --top-level prettier -c -w ./dist/*; eslint -c ../../.eslintrc.cjs ./dist/* --fix",
    "build": "yarn reset; yarn generate; yarn format"
  }
}
