{
  "name": "@learn-react/catalog",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "dependencies": {
    "@learn-react/core": "1.0.0"
  },
  "scripts": {
    "generate": "node ./bin/codegen",
    "develop": "vite",
    "type-check": "tsc --noEmit",
    "start": "run-p \"develop\" \"type-check --watch\" \"generate --watch\"",
    "build": "tsc --noEmit && vite build"
  }
}
