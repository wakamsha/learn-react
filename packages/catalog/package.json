{
  "name": "@learn-react/catalog",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "dependencies": {
    "@learn-react/core": "1.0.0",
    "@learn-react/icon": "1.0.0"
  },
  "scripts": {
    "generate": "node ./bin/codegen",
    "develop": "node ./bin/vite",
    "type-check": "tsc --noEmit",
    "start": "run-p \"generate --watch\" \"type-check --watch\" \"develop {@}\" --",
    "build": "yarn generate && tsc --noEmit && node ./bin/vite -b --"
  }
}
