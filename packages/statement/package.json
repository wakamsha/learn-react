{
  "name": "@learn-react/statement",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "keywords": [
    "mobx",
    "mobx-react",
    "unstated-next",
    "constate"
  ],
  "dependencies": {
    "@learn-react/core": "1.0.0",
    "@learn-react/icon": "1.0.0"
  },
  "scripts": {
    "develop": "   run -T vite serve",
    "type-check": "run -T tsc --noEmit",
    "start": "     run -T run-p \"type-check --watch\" \"develop\"",
    "build": "yarn type-check && vite build",
    "preview": "   run -T vite preview"
  }
}
